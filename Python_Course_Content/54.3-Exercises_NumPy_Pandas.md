# NumPy Exercises

## Exercise Set 1: Array Creation & Data Types

### Exercise 1.1 — Create Arrays

**Task**

1.  Create a NumPy array containing the numbers 1 through 10.
2.  Create a 3×3 array of zeros.
3.  Create an array of 5 evenly spaced numbers between 0 and 100.

**Starter Code**

```python
import numpy as np
```

**Hint**

*   Use `np.arange`, `np.zeros`, and `np.linspace`.

***

### Exercise 1.2 — Data Types

**Task**

1.  Create an array of integers.
2.  Convert it to floats.
3.  Verify the data type before and after.

**Hint**

*   Use `.astype()` and `.dtype`.

***

## Exercise Set 2: Indexing, Slicing, and Reshaping

### Exercise 2.1 — Indexing Practice

Given:

```python
arr = np.array([5, 10, 15, 20, 25, 30])
```

**Task**

1.  Extract the first element.
2.  Extract the last three elements.
3.  Extract every second element.

**Hint**

*   Remember list‑style slicing works the same way.

***

### Exercise 2.2 — 2D Indexing

Given:

```python
matrix = np.array([
    [10, 20, 30],
    [40, 50, 60],
    [70, 80, 90]
])
```

**Task**

1.  Extract the center value (`50`).
2.  Extract the entire second column.
3.  Extract the last two rows.

***

### Exercise 2.3 — Reshaping

**Task**

1.  Create a 1D array with values 0–11.
2.  Reshape it into a 3×4 matrix.
3.  Reshape it again into a 2×6 matrix.

**Hint**

*   The total number of elements must remain constant.

***

## Exercise Set 3: Vectorization & Broadcasting

### Exercise 3.1 — Vectorized Math

**Task**

1.  Create an array of numbers from 1 to 5.
2.  Compute:
    *   The square of each number
    *   The cube of each number
3.  Do this **without** using loops.

***

### Exercise 3.2 — Broadcasting

Given:

```python
prices = np.array([100, 200, 300])
```

**Task**

1.  Apply a 10% tax to each price.
2.  Apply a discount of `[5, 10, 15]` dollars (array subtraction).

**Hint**

*   NumPy automatically broadcasts scalars and compatible arrays.

***

## Exercise Set 4: Statistics & Aggregations

### Exercise 4.1 — Basic Statistics

Given:

```python
data = np.array([12, 15, 20, 10, 18, 25])
```

**Task**

1.  Compute the mean, median, and standard deviation.
2.  Find the minimum and maximum values.

***

### Exercise 4.2 — Axis-Based Operations

Given:

```python
scores = np.array([
    [80, 90, 85],
    [75, 95, 88],
    [90, 85, 92]
])
```

**Task**

1.  Compute the average score per student (row).
2.  Compute the average score per exam (column).

**Hint**

*   Use the `axis` parameter.

***

# Pandas Exercises

## Exercise Set 1: Series and DataFrames

### Exercise 1.1 — Create a Series

**Task**

1.  Create a Pandas Series containing temperatures for five days.
2.  Assign meaningful index labels (e.g., `"Mon"`, `"Tue"`).

***

### Exercise 1.2 — Create a DataFrame

**Task**
Create a DataFrame with:

*   Columns: `name`, `age`, `city`
*   At least 3 rows of data

**Hint**

*   Use a dictionary of lists.

***

## Exercise Set 2: Loading and Inspecting Data

### Exercise 2.1 — Data Inspection

Given a CSV file named `sales.csv`:

**Task**

1.  Load the file.
2.  Display the first 5 rows.
3.  Check column types and missing values.
4.  Get summary statistics for numeric columns.

**Methods to Use**

*   `.read_csv()`
*   `.head()`
*   `.info()`
*   `.describe()`

***

## Exercise Set 3: Indexing, Selecting, and Filtering

### Exercise 3.1 — Column Selection

Given:

```python
df = pd.DataFrame({
    "product": ["A", "B", "C", "D"],
    "price": [10, 15, 8, 12],
    "quantity": [100, 150, 200, 90]
})
```

**Task**

1.  Select only the `price` column.
2.  Select both `product` and `quantity`.

***

### Exercise 3.2 — Filtering Rows

**Task**

1.  Filter products with price greater than 10.
2.  Filter products with quantity less than 100.

**Hint**

*   Use boolean conditions inside `df[...]`.

***

## Exercise Set 4: Handling Missing Data

### Exercise 4.1 — Missing Values

Given:

```python
df = pd.DataFrame({
    "name": ["Alice", "Bob", "Charlie"],
    "salary": [70000, None, 90000]
})
```

**Task**

1.  Identify missing values.
2.  Replace missing salaries with the average salary.
3.  Verify no missing values remain.

***

## Exercise Set 5: Grouping and Aggregation

### Exercise 5.1 — GroupBy Basics

Given:

```python
df = pd.DataFrame({
    "department": ["IT", "HR", "IT", "Finance", "HR"],
    "salary": [80000, 60000, 85000, 90000, 65000]
})
```

**Task**

1.  Compute average salary per department.
2.  Compute both mean and max salary per department.

***

### Exercise 5.2 — Transform

**Task**

1.  Add a column showing the department’s average salary.
2.  Compare each employee’s salary to the department average.

**Hint**

*   Use `.groupby().transform()`.

***

## Exercise Set 6: Merging and Reshaping

### Exercise 6.1 — Merge DataFrames

Given:

```python
employees = pd.DataFrame({
    "emp_id": [101, 102, 103],
    "name": ["Alice", "Bob", "Charlie"]
})

departments = pd.DataFrame({
    "emp_id": [101, 102, 103],
    "department": ["IT", "HR", "Finance"]
})
```

**Task**

1.  Merge the two DataFrames on `emp_id`.
2.  Verify that all employee data is preserved.

***

## Capstone Mini‑Exercises (Optional)

### NumPy Mini‑Challenge

*   Simulate exam scores for 100 students (random integers 50–100)
*   Compute:
    *   Mean score
    *   Standard deviation
    *   Number of students who passed (≥70)

***

### Pandas Mini‑Challenge

Using a realistic dataset (CSV):

*   Load the data
*   Clean missing values
*   Filter for a meaningful subset
*   Group and aggregate metrics
*   Answer a real question (e.g., “Which category performs best?”)

***

## Teaching Tip (for Instructors)

Encourage learners to:

*   Print intermediate results
*   Check `.shape` frequently
*   Experiment by “breaking” code intentionally

